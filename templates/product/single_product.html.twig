<a href="{{ path('product', {'slug' : product.slug}) }}">
    <img src="/uploads/media/{{ product.illustration }}" class="card-img-top" alt="{{ product.name }}">
</a>
<div class="card-body text-center">
    <h5>{{ product.name|upper }}</h5>
    <p class="card-text"><strong>Prix: {{( product.price / 100)|number_format (2, ',', ' ')}} €</strong></p>
</div>